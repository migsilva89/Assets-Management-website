/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'static.vecteezy.com', 'tailwindui.com', 'localhost']
  }
}

module.exports = nextConfig
